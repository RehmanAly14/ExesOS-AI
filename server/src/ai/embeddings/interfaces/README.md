# Interfaces

Contains TypeScript interfaces that define the contracts for the Embedding Pipeline. This ensures loose coupling between document parsing, chunking, embedding providers, and vector storage.
