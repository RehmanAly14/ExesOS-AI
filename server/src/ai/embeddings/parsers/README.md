# Parsers

The entry point for raw business documents. The responsibility of parsers is to convert various file types (e.g., PDF, Word, Markdown) into a standardized plain text format and extract preliminary metadata (e.g., page numbers, titles) before passing it downstream to the chunker.
